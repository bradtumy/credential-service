{
  "name": "@id2me/credential-sdk",
  "version": "0.1.0",
  "description": "Node.js SDK for the Credential Service issuer, verifier, and gateway APIs.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test dist-tests/tests/*.js"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/example/credential-service.git"
  },
  "keywords": [
    "verifiable-credentials",
    "sd-jwt",
    "jwt",
    "identity",
    "agent"
  ],
  "author": "ID2ME",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^22.9.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
