apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: credential-service

resources:
  - namespace.yaml
  - postgres.yaml
  - issuer.yaml
  - verifier.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: credential-service
  app.kubernetes.io/managed-by: kustomize
